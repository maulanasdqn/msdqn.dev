import { BaseLayout } from "@/modules/common";
import { FC, ReactElement } from "react";

export const ETwitterEmbedModule: FC = (): ReactElement => {
  return (
    <BaseLayout>
      <span>Error was Happen</span>
    </BaseLayout>
  );
};
