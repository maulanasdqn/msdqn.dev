import { FC, ReactElement } from "react";

const AboutPages: FC = (): ReactElement => {
  return (
    <section className="flex flex-col items-center justify-center w-full h-screen">
      <span>about page</span>
    </section>
  );
};

export default AboutPages;
