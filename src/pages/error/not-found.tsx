import { NotFoundErrorModules } from "@/modules";
import { FC, ReactElement } from "react";

const NotFoundErrorPages: FC = (): ReactElement => {
  return <NotFoundErrorModules />;
};

export default NotFoundErrorPages;
